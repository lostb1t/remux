use crate::clients;
use crate::components;
use crate::Route;
use dioxus::logger::tracing::{error, info};
use dioxus::prelude::*;


#[component]
pub fn Settings() -> Element {
    rsx! {
        // components::Hero {}
        //Servers{}
       // "heya partner"
    }
}

